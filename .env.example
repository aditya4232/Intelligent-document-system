# ─── Frontend environment variables ───────────────────────────────────────────
# Copy this file to .env.local for local development.
# Never commit .env.local — it is listed in .gitignore.

# URL of the running FastAPI backend.
# • Local dev  : leave as-is (Vite proxy handles /ask-recruiter automatically)
# • Production : set to your deployed backend URL, e.g. https://api.yourdomain.com
VITE_API_URL=http://127.0.0.1:8000

# Base path when deploying to GitHub Pages (set automatically by CI).
# • GitHub Pages : /your-repo-name/
# • Netlify / root domain : /   (leave commented out)
# VITE_BASE_PATH=/documind-in/
